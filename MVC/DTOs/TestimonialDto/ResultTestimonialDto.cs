namespace MVC.DTOs.TestimonialDto;

public record ResultTestimonialDto(
                                   int Id,
                                   string Surname,
                                   string Title,
                                   string Comment,
                                   string ImageUrl);

